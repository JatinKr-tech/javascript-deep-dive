//export default
/*
export default class User { // just add "default"
  constructor(name) {
    this.name = name;
  }
}
  */
export default class { // just add "default"
  constructor(name) {
    this.name = name;
  }
}